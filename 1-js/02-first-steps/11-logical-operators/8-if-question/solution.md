答案是：第一和第三個會執行。

詳解：

```js run
// 執行
// -1 || 0 = -1，真值
if (-1 || 0) alert( 'first' );

// 不執行
// -1 && 0 = 0，虛值
if (-1 && 0) alert( 'second' );

// 執行
// && 運算子比 || 優先權更高
// 所以 -1 && 1 先執行，得到這串運算：
// null || -1 && 1  ->  null || 1  ->  1
if (null || -1 && 1) alert( 'third' );
```

